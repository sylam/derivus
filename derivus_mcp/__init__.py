"""The derivus MCP binding - a sibling package to `derivus_bloomberg`: shipped in the same wheel,
holding no logic, importing `requests` + `mcp` and never the engine. `DV_MCP` serves it over stdio.
"""
